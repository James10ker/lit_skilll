# 使用 tools/md_to_docx.py、docx_to_md.py、pptx_to_md.py、math_render.py 时安装
python-docx>=1.1.0
# 定稿图示（mermaid → PNG）须 Node：在 tools/ 执行 npm install 或 npx，见 tools/README.md
mammoth>=1.6.0
python-pptx>=0.6.21
matplotlib>=3.8.0
PyMuPDF>=1.24.0

# 外部文献数据库检索通常由 Agent 的联网能力完成；本文件只保留本地格式转换依赖。
