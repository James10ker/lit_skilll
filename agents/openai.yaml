interface:
  display_name: "论文综述复现生成"
  short_description: "从论文独立生成经质检的 LaTeX 综述与图表"
  brand_color: "#2563EB"
  default_prompt: "Use $literature-review-skill to independently synthesize the supplied papers into a citation-grounded review, generate and validate the required figures, self-correct until the quality gates pass, and deliver a complete LaTeX project."
policy:
  allow_implicit_invocation: true
